- use services instead of mockups (forwardings, mailboxes)
- edit dialogs
- new forwarding/mailbox
- delete forwarding/mailbox
- include icons
- move change password from the start page to somewhere else
- use nav bar input to log in
